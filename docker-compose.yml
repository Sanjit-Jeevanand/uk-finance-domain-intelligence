services:
  finance-dis:
    image: ghcr.io/sanjit-jeevanand/uk-finance-domain-intelligence/finance-dis:${IMAGE_TAG:-latest}
    container_name: finance-dis
    ports:
      - "8000:8000"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}